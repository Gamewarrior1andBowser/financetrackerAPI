@{  ViewData["Title"] = "Categories";}

<div class="container">

    <h1 class="mb-4"> Categories </h1>

    <div class="mb-3">
        <a asp-controller="CategoryPage" asp-action="Create" class="btn btn-primary" > Add Category </a>
    </div>

    <table class="table table-bordered">
        <thead class="table-primary">
            <tr>
                <th> Category Name </th>
                <th>  Actions </th>
            </tr>
        </thead>

        <tbody id="categoryTable"> </tbody>
        @*For: Category Names: The names of the new categories made by the user*@
        @*For: Actions: Icons of the Delete and Edit functions to allow the user Delete and or edit a category*@
    </table>
    <a asp-controller="Dashboard"
       asp-action="Index" class="btn btn-primary">Go to Dashboard</a>
</div>

@section Scripts {

    <script>
        const token = localStorage.getItem("token");

        fetch("/api/Category", {  headers: { "Authorization": "Bearer " + token }
        })
            .then(response => response.json())

            .then(data => {

                let rows = "";

                data.forEach(category => {
                    rows += `

                    <tr>
                        <td> ${category.name} </td>
                        <td> ${category.type} </td>
                        <td>
                            <button class="btn btn-warning btn-sm"> Edit  </button>
                            <button class="btn btn-danger btn-sm">  Delete  </button>
                        </td> 
                     </tr>  `;
                });

                document.getElementById("categoryTable").innerHTML = rows;

            })

            .catch(error => {
                console.log(error);
            });
    </script>
}