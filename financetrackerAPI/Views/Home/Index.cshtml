@{
    ViewData["Title"] = "Home";
}

<div class="home-page">

    <section class="home-hero">
        <div class="home-hero-text">
            <span class="home-badge">
                <i class="bi bi-stars"></i> Built for everyday budgets
            </span>

            <h1>Know exactly where your money goes.</h1>

            <p class="home-hero-lead">
                Finance Tracker turns scattered spending into a clear picture —
                log transactions, set budgets, and watch your savings goals
                move in the right direction.
            </p>

            <div class="home-hero-actions">
                <a asp-controller="AuthPage" asp-action="Register"
                   class="home-btn home-btn-primary">
                    <i class="bi bi-rocket-takeoff-fill me-2"></i> Get Started Free
                </a>

                <a asp-controller="AuthPage" asp-action="Login"
                   class="home-btn home-btn-ghost">
                    Sign In
                </a>
            </div>
        </div>

        <div class="home-hero-card">
            <h3>Why people stick with it</h3>
            <p>
                No spreadsheets, no guesswork — just a running total of what's
                coming in, what's going out, and what's left over.
            </p>

            <ul class="home-hero-points">
                <li><i class="bi bi-check-circle-fill"></i> Every transaction in one place</li>
                <li><i class="bi bi-check-circle-fill"></i> Budgets that warn you before you overspend</li>
                <li><i class="bi bi-check-circle-fill"></i> Savings goals you can actually see progress on</li>
            </ul>
        </div>
    </section>

    <section class="home-feature-grid">

        <a asp-controller="AuthPage" asp-action="Register">
            <div class="home-feature-card">
                <div class="home-feature-icon purple">
                    <i class="bi bi-receipt me-2"></i>
                </div>
                <h4>Track Transactions</h4>
                <p>Add income and expenses as they happen, so nothing slips through the cracks.</p>
            </div>
        </a>

        <a asp-controller="AuthPage" asp-action="Register">
            <div class="home-feature-card">
                <div class="home-feature-icon amber">
                    <i class="bi bi-tags-fill"></i>
                </div>
                <h4>Sort Category</h4>
                <p>Group spending into categories that actually mean something to you.</p>
            </div>
        </a>

        <a asp-controller="AuthPage" asp-action="Register">
            <div class="home-feature-card">
                <div class="home-feature-icon blue">
                    <i class="bi bi-bar-chart-fill"></i>
                </div>
                <h4>Finanace Reports</h4>
                <p>Reports and trends make it easy to spot habits before they become problems.</p>
            </div>
        </a>

    </section>

    <section class="home-preview-intro">
        <h2>Take a look inside</h2>
        <p>A sample of what your dashboard looks like once your data is in.</p>
    </section>

    <section class="home-summary-cards">

        <div class="home-summary-card income">
            <span>Total Income</span>
            <strong id="income">$0</strong>
        </div>

        <div class="home-summary-card expense">
            <span>Total Expenses</span>
            <strong id="expenses">$0</strong>
        </div>

        <div class="home-summary-card balance">
            <span>Remaining Balance</span>
            <strong id="balance">$0</strong>
        </div>

    </section>

    <section class="home-panel-grid">

        <div class="home-panel">
            <h3>
                <i class="bi bi-wallet2 me-2"></i> Budget Preview
            </h3>
            <hr />

            <div class="home-panel-row">
                <span>Monthly Budget</span>
                <strong id="budgetlimits">$0</strong>
            </div>

            <div class="home-panel-row">
                <span>Amount Used</span>
                <strong id="budgetUsed">$0</strong>
            </div>

            <div class="home-panel-row">
                <span>Remaining</span>
                <strong class="positive" id="budgetRemaining">$0</strong>
            </div>

            <div class="home-progress">
                <div class="home-progress-bar" id="budgetProgressBar"></div>
            </div>

            <span class="home-progress-label" id="budgetPercentage">
                0% used
            </span>
        </div>

        <div class="home-panel">
            <h3>
                <i class="bi bi-piggy-bank-fill me-2"></i> Savings Goal
            </h3>
            <hr />

            <div class="home-panel-row">
                <span>Goal Amount</span>
                <strong id="savingGoal">$0</strong>
            </div>

            <div class="home-panel-row">
                <span>Current Savings</span>
                <strong class="accent" id="savingCurrent">$0</strong>
            </div>

            <div class="home-panel-row">
                <span>Remaining</span>
                <strong class="negative" id="savingRemaining">$0</strong>
            </div>

            <div class="home-progress">
                <div class="home-progress-bar savings" id="savingProgressBar"></div>
            </div>

            <span class="home-progress-label" id="savingProgress">
                0% reached
            </span>
        </div>

    </section>

    <section class="home-table-card">
        <h3>Recent Transactions Preview</h3>

        <table class="home-table">
            <thead>
                <tr>
                    <th>Category</th>
                    <th>Type</th>
                    <th>Amount</th>
                    <th>Date</th>
                </tr>
            </thead>

            <tbody id="demoTransactions"></tbody>
        </table>
    </section>

</div>

@section Scripts {
    <script>
        async function loadDemoData() {
            const response = await fetch("/data/demo-data.json");
            const data = await response.json();

            document.getElementById("income").innerHTML = "$" + data.income;
            document.getElementById("expenses").innerHTML = "$" + data.expenses;
            document.getElementById("balance").innerHTML = "$" + data.balance;

            document.getElementById("budgetlimits").innerHTML =
                "$" + data.budget.monthlylimits;

            document.getElementById("budgetUsed").innerHTML =
                "$" + data.budget.used;

            document.getElementById("budgetRemaining").innerHTML =
                "$" + data.budget.remaining;

            const budgetPercent = data.budget.usagePercentage;

            document.getElementById("budgetPercentage").innerHTML =
                budgetPercent + "% used";

            document.getElementById("budgetProgressBar").style.width =
                Math.min(budgetPercent, 100) + "%";

            document.getElementById("savingGoal").innerHTML =
                "$" + data.savingsGoal.goal;

            document.getElementById("savingCurrent").innerHTML =
                "$" + data.savingsGoal.current;

            document.getElementById("savingRemaining").innerHTML =
                "$" + data.savingsGoal.remaining;

            const savingPercent = data.savingsGoal.progress;

            document.getElementById("savingProgress").innerHTML =
                savingPercent + "% reached";

            document.getElementById("savingProgressBar").style.width =
                Math.min(savingPercent, 100) + "%";

            let rows = "";

            data.transactions.forEach(transaction => {
                rows += `
                            <tr>
                                <td>${transaction.category}</td>
                                <td>
                                    <span class="home-type-pill ${transaction.type.toLowerCase()}">
                                        ${transaction.type}
                                    </span>
                                </td>
                                <td>$${transaction.amount}</td>
                                <td>${transaction.date}</td>
                            </tr>
                        `;
            });

            document.getElementById("demoTransactions").innerHTML = rows;
        }

        loadDemoData();
    </script>
}